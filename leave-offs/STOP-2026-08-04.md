# STOPPED MID-TASK — 2026-08-04

Read this with `SESSION-RESUME.md`. That file describes the project; this one describes an
interrupted task and a data-loss incident on the same day.

---

## 1. The data-loss incident — read first

**The repository was copied from `C:\Users\athet\OneDrive\Documents\FightGame` to
`C:\Users\athet\Documents\FightGame` on 2026-08-04. Both copies still exist.** The user chose
to keep the OneDrive copy.

**During that move, OneDrive rolled two files backward.** Observed directly:
`design/goal-plan.md` was **59,247 bytes at 21:08**, and afterwards was **44,142 bytes at
21:02** — smaller *and* older. `leave-offs/SESSION-RESUME.md` likewise reverted to its
2026-08-03 state, discarding an edit made minutes earlier.

**What was lost:** `design/goal-plan.md` **sections 5–8** — the ranking rule, the coupled
groups, the recommended next dispatch, and the stop notice. Roughly 224 lines. **Sections 1–4
survive** (488 lines), with section 4's ranked table cut off mid-way.

**What survived:** both `assignment-05/README.md` edits, in both copies.

**Recovery not yet attempted:** **OneDrive version history** on `design/goal-plan.md` is the
best shot at the 59,247-byte version — right-click the file → *Version history*, or the same
on onedrive.com, or the OneDrive recycle bin. No conflict copies exist on disk and the
subagent transcript file is empty, so there is no other source. **If version history fails,
sections 5–8 must be regenerated by re-running the `goal-planner`.**

**Lesson for whoever is next: commit before moving anything.** Git was the only thing that
would have made this a non-event, and the work was uncommitted.

---

## 2. The interrupted task

Three parts were requested against `assignment-05/`. Two are done, one is half done.

### Done — in the working tree

1. **Deleted a false claim.** `assignment-05/README.md` §3 used to open *"There is no goal
   primitive in Claude Code."* **That is false — `/goal` shipped in Claude Code 2.1.139**
   (`https://code.claude.com/docs/en/goal`). It is gone, replaced by a plain statement of what
   a goal is here. **The opposite claim was deliberately not written in its place** — §3 is
   about how *this* project enforces goals with `entry_gate.py` and `exit_gate.py`, and that
   stands on its own.
2. **Added "The short version"** to `assignment-05/README.md`, right after the deliverables
   tree: the two gates in plain language, why that pair is a goal (the check runs outside the
   agent, so the agent cannot talk past it), that it predates the class coverage and was kept,
   and what `agent/` and `evidence/` hold.

Both edits are confirmed present in both copies of the repo.

### Half done — requirement 5, "the agent generates code for a missing feature"

**The `goal-planner` was run for the first time, 2026-08-04.** It wrote `design/goal-plan.md`
with all eight contracted sections — inputs read, scope restatement, decided-vs-open counts,
**79 ranked items each carrying a `build-sequence.md` step id**, the ranking rule, coupled
groups, the recommended dispatch, and the stop notice. It stopped correctly at **item 2
(Q1, player max health)** as a design question, and wrote nothing outside that file.

**Then it was killed before writing §9, and then sections 5–8 were lost to the rollback
above.** Also never written: `leave-offs/goal-planner.md`, and any generated artifact.

`design/goal-plan.md` line 52 reserves §9 for the attack-row task, so the file promises a
section it does not have. **Either write §9 or delete the promise.**

---

## 3. The finding that changes requirement 5 — verified, act on it

The dispatch asked the planner to fill a blank field in a `Planned` / `false` row of
`data/unreal/DT_VanguardAttacks.csv` using a value already **APPROVED** in
`design/decisions.md`. **That route cannot produce anything, and it is not a near miss:**

- Rows B and C have **no blank fields at all** except the four asset/socket columns; row D
  adds a blank `TrackingRule`.
- `docs/unreal/VANGUARD_ATTACK_ROW_CONTRACT.md` §4 **forbids populating** `MontageAsset`,
  `TelegraphVfxAsset`, `TelegraphAudioAsset` and `HitTraceSocket` in this pass, and
  `tools/validate_vanguard_attack_csv.py` enforces it as a hard blank check —
  **populating any of them fails the validator.**
- Row D's blank `TrackingRule` is **deliberate, not missing**: the contract says only B and C
  carry a tracking-lock rule, and "A and D leave this blank."
- **No APPROVED entry in `design/decisions.md` corresponds to any CSV column.** The approved
  set is Q22, V1–V5, item 20 and item 28. Everything CSV-shaped — Q3, Q10, Q12, Q13, Q25's 26
  values — is **PROPOSED**, and PROPOSED is not decided.

So the CSV yields a correct refusal, not a generated feature, and requirement 5 would stay
unmet on that route alone.

---

## 4. The decision already taken, and the resume list

**The user chose "both"** — document the refusal *and* generate a feature from a decision that
really is approved.

1. **Recover or regenerate `design/goal-plan.md` sections 5–8** (§1 above).
2. **Write §9** — the CSV rows identified field by field, ranked by the lowest
   `build-sequence.md` step each blocks (they land at `M2-04`), and the refusal in §3 stated
   plainly with the contract and validator cited. Nothing generated, and why.
3. **Generate the missing feature from an APPROVED decision instead:** **Q22 →
   `BP_HealthComponent`**, build step **`M1-08`**. `MinHealthFloor = 1` from `BeginPlay`,
   lowered to `0` only by `ClashSuccess()` immediately before it applies lethal damage.
   APPROVED by the designer of record 2026-08-02, fully specified, **invents no number**, and
   the component does not exist yet. **Blueprint-first** per the approved framework
   evaluation — generate the node-level graph, not C++. Read `build-sequence.md` `M1-08` and
   `combat-integration-plan.md` first so it extends them rather than contradicting them.
4. **Write `leave-offs/goal-planner.md`** — the `status: complete` line **last**, only once the
   artifacts are really on disk.
5. **Add the README section** naming the feature generated, why that one was selected, and what
   is still open — and say the CSV route was tried first and correctly refused, because that is
   the stronger evidence that the gates work.
6. **Commit and push to `origin main`.**

**Do not try to resume the killed subagent by ID** — that session is gone and its transcript
file is empty. Spawn the `goal-planner` fresh.

---

## 5. THE UNREAL PROJECT NOW EXISTS — `CLAUDE.md` is stale on this

`CLAUDE.md` says *"There is no `.uproject` and no `Content/` anywhere in this repo — nothing
exists in Unreal yet."* **That is no longer true as of 2026-08-04.**

`C:\Users\athet\Documents\FightGame\FightGame\` is a **UE 5.8 project**, untracked by git:

| | |
|---|---|
| `FightGame.uproject` | `"EngineAssociation": "5.8"` — correct engine |
| Plugins enabled | `ModelingToolsEditorMode`, `GameplayStateTree`, **`ModelContextProtocol`** |
| Content | Third Person template + **`Variant_Combat`** (its own `Lvl_Combat.umap`, Anims, Blueprints, Input, UI, VFX), `Characters/Mannequins`, `LevelPrototyping`, `Input/` |
| Size | `Content` 135 MB · `Intermediate` 124 MB · `Saved` 3.8 MB · `DerivedDataCache` 1.6 MB |

**Two things follow immediately.**

1. **`ModelContextProtocol` is enabled**, which is the engine half of `TODO.md` **item 1**
   (Unreal MCP not connected) — the item blocking all 63 build steps. **Item 1 is not closed
   by this**; its acceptance condition is a proven round-trip from this session, not a plugin
   checkbox. But the prerequisite is in place and item 1 should be re-tested, not assumed.
2. **The move out of OneDrive was the right call.** Unreal projects and OneDrive sync are a
   known-bad pairing — `Intermediate/` and `Saved/` churn constantly and `DerivedDataCache`
   is large and disposable. The rollback in §1 is exactly that failure mode showing up.

**Nothing has been committed and nothing was changed inside the project.** Before it is:

- The root `.gitignore` already covers `Binaries/ Build/ DerivedDataCache/ Intermediate/
  Saved/` and the patterns are depth-independent, so they apply to the nested project
  correctly. **`Content/` is not ignored — committing `FightGame/` as-is adds ~135 MB** of
  mostly Epic template assets to the repo. That is a deliberate decision, not a default.
- **Milestone claim to re-derive, not to assume.** A project created from the Third Person +
  Combat template is not M1 complete. What is in `Variant_Combat` is Epic's template content,
  not authored Ascendant Impact work. Someone should walk `build-sequence.md` M1 against what
  is actually in `Content/` before any milestone statement is updated.
- **`CLAUDE.md` needs its M1 paragraph corrected** once that walk is done.

---

## 6. Which copy is authoritative

**`C:\Users\athet\Documents\FightGame` is the working repo going forward.** The OneDrive copy
is kept deliberately but is **not** to be edited — two live copies of one git repo is how the
wrong one gets committed. Both are at `b2c2a8a`, level with `origin/main`, with
`assignment-05/README.md` modified and `design/goal-plan.md` untracked.
