# Design reference images

Hand-collected visual reference. Distinct from `gdd/reference/`, which is extracted from
the GDD PDF and is never hand-edited.

| File | What it is |
|---|---|
| `ref-arena-01-octagon.jpg` | The arena the octagon was designed from. `Tools/ArenaPipeline/build_octagon_arena.py`, `build_octagon_detail.py` and `build_octagon_tiers.py` all derive their proportions from it — the two-tier gallery, the truss walls on facets 0 and 4, and the diagonal ramp slabs on facets 1, 3, 5 and 7. Moved into the repo 2026-08-27 from a working folder outside it. |
